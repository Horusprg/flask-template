document.addEventListener('DOMContentLoaded', () => {
    const button = document.getElementById('exampleButton');
    button.addEventListener('click', () => {
        alert('Button clicked! Welcome to the Flask Template Example.');
    });
});
